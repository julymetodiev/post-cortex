[package]
name = "post-cortex-memory"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Orchestrator for post-cortex — ConversationMemorySystem ties storage + embeddings + graph + session + summary into a single lock-free memory hierarchy with async pipelines."
keywords = ["post-cortex", "memory", "orchestrator", "conversation", "ai"]
categories = ["data-structures", "concurrency"]
readme = "README.md"
documentation = "https://docs.rs/post-cortex-memory"

[dependencies]
# Phase 3d fills this in. Phase 3b stub keeps the workspace loadable.
